version: '2'
services:
  broadcast:
    build: ./service
    restart: always

  nginx:
    image: nginx
    depends_on:
      - broadcast
    ports:
      - "127.0.0.1:30009:80"
    restart: always
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro