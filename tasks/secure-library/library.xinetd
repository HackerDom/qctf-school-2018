service library
{
  disable = no
  socket_type = stream
  protocol = tcp
  wait = no
  user = root
  bind = 0.0.0.0
  server = /chroot_run.sh
  type = UNLISTED
  port = 9999
  log_type = FILE /var/xinetd.log
  log_on_success = HOST PID DURATION EXIT
  log_on_failure = HOST ATTEMPT
}
